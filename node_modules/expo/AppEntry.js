import { KeepAwake, registerRootComponent } from 'expo';
import App from '../../App';
import MainScreenNavigator from '../../models/home/homescreen';
//import HomeSwiper from '../../models/home/homeSwiper';

if (__DEV__) {
  KeepAwake.activate();
}
registerRootComponent(App);