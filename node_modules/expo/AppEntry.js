import { KeepAwake, registerRootComponent } from 'expo';
//import App from '../../App';
import MainScreenNavigator from '../../models/home/homescreen';

if (__DEV__) {
  KeepAwake.activate();
}

registerRootComponent(MainScreenNavigator);
